# A user "aladdin" with password "open_sesame" is prompted for his/her username 
# and password and they are stored in variables username and password.

# What is the correct code to check if the user has entered both correctly?
# a. if username == "aladdin" and password == "open_sesame": # Correct
# b. if username = "aladdin" or password = "open_sesame":
# c. if username == "aladdin" or password == "open_sesame":
# d. if username = "aladdin" and password = "open_sesame":


username = "aladdin"
password = "open_sesame"

if username == "aladdin" and password == "open_sesame":
    print("Access granted")

# if username = "aladdin" or password = "open_sesame":
# if username == "aladdin" or password == "open_sesame":
# if username = "aladdin" and password = "open_sesame":