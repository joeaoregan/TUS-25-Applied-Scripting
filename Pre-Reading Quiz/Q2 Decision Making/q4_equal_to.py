# A00258304

# Which of the following is the correct statement (in Python!) to check if percent_free is equal to zero?

# a. if (percent_free == 0)
# b. if percent_free == 0: # True
# c. if percent_free = 0:
# d. if (percent_free = 0)

percent_free = 0
if percent_free == 0:
    print(True)